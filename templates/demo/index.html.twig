{% extends 'base.html.twig' %}

{% block title %}Stimulus Demo{% endblock %}

{% block body %}
    {{ include('demo/_header.html.twig') }}
    <div class="container">
        <div class="row">
            <section class="col-9">
                <h2>Hello, I'm a section</h2>
                {{ include('demo/_counter.html.twig') }}
            </section>
            <aside class="col">
                <p>I'm an aside</p>
                {{ include('demo/_counter.html.twig') }}
            </aside>
        </div>
        <div class="row">
            <hr>
            <div data-controller="really-basic-counter"></div>
        </div>
        <div class="row">
            <hr>
            <h2>{{ product.name }}</h2>
            <small>{{ product.sku }}</small>
            {{ include('demo/_add_to_cart.html.twig') }}
        </div>
    </div>
{% endblock %}