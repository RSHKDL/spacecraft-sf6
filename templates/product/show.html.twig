{% extends 'base.html.twig' %}

{% block title %}Our Shop : {{ product.name }}{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <h1>{{ product.name }}</h1>
            <small><b>#{{ product.id }}</b> {{ product.sku }}</small>
            <p>A fake description</p>
        </div>
    </div>
{% endblock %}